# SPEC003: 快楽・絶頂システムMOD

| 項目 | 内容 |
|---|---|
| 状態 | 実装可能 |
| 最終更新日 | 2026-08-08 |
| 対象 | SiNiSistar2 Windows x64版 |
| 対象Unityランタイム | Unity 2022.3.62f2 / IL2CPP |
| `GameAssembly.dll` SHA-256 | `B869493305BBE587598C8709E7FE271F00D79D37803C6A8241946D6A6297499D` |
| `global-metadata.dat` SHA-256 | `A56278D0162B6C148312B56FBE208B54BA9AF2D3BAD609EBF9349B7AE7DDC84B` |
| プラグインGUID | `community.sinisistar2.pleasure` |
| 共存対象 | [SPEC001](SPEC001.md) EDI連動MOD、[SPEC002](SPEC002.md) 難易度上昇MOD |
| 想定読者 | MOD実装者、テスト担当者、バランス調整担当者 |
| 開発ブランチ | `feature/difficulty-mod` |

## 1. 概要

### 1.1 背景と問題

本作の拘束は、HPを削られ、HP0でゲームオーバーになる形で決着する。したがって拘束の敗北条件は体力管理の一部であり、拘束そのものが何を意味するかとは結び付いていない。捕食や暴力を行う敵と、性的な行為を行う敵が、同じ「HPを削る」という一つの経路で処理されている。

その結果、次が成立しない。

- 性的な行為を受け続けたことの蓄積が、ゲーム上の状態として存在しない。回復すれば元に戻る。
- 敵の性質の違いが、拘束中の脅威の質の違いとして現れない。
- 拘束からの敗北が、体力ではなく「どれだけ受け入れてしまったか」で決まる設計になっていない。

本MODは、拘束中の敗北条件をHPから切り離し、**快楽**と**絶頂回数**という別の軸へ置き換える。あわせて、受け続けたことが戻らない形で蓄積する**感度**を導入する。

イベントでしか発生しない上位デバフ（`AbnormalType.BreastSuper`）は、ローカライズもセリフも揃った完成済みの状態異常でありながら、通常プレイでは到達しない。本MODはこれを新しい蓄積系の到達点として通常プレイへ開放する。

### 1.2 目的

- 拘束中にHPが0へ達してゲームオーバーになる経路を撤廃する。拘束中の敗北はHPで決まらないようにする。
- 性的な攻撃を行う敵に対してのみ上昇する快楽ゲージを追加する。捕食・暴力を行う敵では上昇させない。
- 快楽が上限に達したとき絶頂させ、画面演出で明示する。
- 拘束によるゲームオーバーを、HPではなく絶頂回数が限界に達したことで成立させる。限界回数はプレイヤーのステータスに応じて増える。
- 絶頂回数を、セーブポイントのアクティベートでリセットできるようにする。
- 受け続けたことが戻らない形で蓄積し、絶頂しやすくなる感度を追加する。デバフの治療で減らない。
- `BreastSuper` を通常プレイでも到達し得る状態異常にする。
- SPEC001・SPEC002 と同一ゲームルートで共存し、いずれの動作も壊さない。
- ゲームのセーブデータを書き換えず、MODを取り外せば痕跡を残さない。

### 1.3 成功条件

1. 拘束中にHPが0へ達しない。HPが尽きたことを理由に拘束中のゲームオーバーが発生しない。
2. 拘束外でHPが0になった場合のゲームオーバーは、MOD導入前と変わらない。
3. 性的な攻撃を行う敵に拘束されている間だけ快楽が上昇し、捕食・暴力のみを行う敵では上昇しない。
4. 快楽が上限に達すると絶頂が発生し、画面演出で識別でき、快楽が0へ戻り、絶頂回数が1増える。
5. 絶頂回数が限界に達すると、HPの残量にかかわらず拘束によるゲームオーバーが成立する。
6. 限界回数がプレイヤーのステータスに応じて増える。
7. セーブポイントをアクティベートすると絶頂回数が0へ戻る。感度は戻らない。
8. 感度は絶頂と性的攻撃の被弾で増加し、いかなる操作でも減少しない。デバフを治療しても減らない。
9. 感度が高いほど、同じ攻撃で快楽がより多く上昇する。
10. `BreastSuper` が通常プレイ中に発生し得る。
11. 感度と絶頂回数がセーブ・ロードをまたいで保存され、ロード後に保存時点の値へ戻る。
12. MODを取り外したとき、ゲームのセーブデータに本MOD由来の差分がない。
13. SPEC001・SPEC002 が同時に動作している状態で、いずれの動作も停止しない。

## 2. スコープ

### 2.1 対象

- 拘束中の `RemainHp1` 寄与によるHP0到達の抑止。
- 快楽ゲージの導入、上昇条件、減衰、上限。
- 性的攻撃と非性的攻撃の判別。
- 絶頂の発生、画面演出、絶頂回数の加算。
- 絶頂限界回数の算出と、限界到達によるゲームオーバー。
- セーブポイントでの絶頂回数リセット。
- 感度の蓄積、快楽上昇への反映、減少の禁止。
- 通常プレイ中の `BreastSuper` 付与。
- 感度と絶頂回数のセーブスロット単位の永続化と、ゲームのセーブ・ロードとの同期。
- BepInEx `ConfigEntry` による調整値。
- SPEC001・SPEC002 との共存条件。

### 2.2 非対象

- 拘束中のHPダメージそのものの停止。ダメージ、表示、演出は従来どおり発生する。撤廃するのは「HPが0へ達すること」だけである。
- 拘束外のHP0によるゲームオーバー、ゲームオーバー後の復帰処理、ペナルティの変更。
- ゲームのセーブデータファイル、`SaveFlagAndVariable`、`GameAssembly.dll`、`global-metadata.dat`、ゲームアセットの書き換え。
- ゲーム本体の演出資産の改変、新規スプライト・アニメーションの追加。
- 快楽・感度・絶頂回数のゲーム内設定画面。調整はBepInEx設定で行う。
- 敵のAI、行動パターン、拘束の開始条件の変更。
- SPEC002 の3機構（状態異常の強化、無力化窓、復帰遅延）の変更。
- 既存セーブへの遡及。MOD導入前の被弾履歴は感度に反映しない。
- `BreastSuper` 以外のイベント限定コンテンツの開放。

### 2.3 前提と制約

- MODはBepInEx 6 IL2CPP環境で動作し、HarmonyXを実行基盤として利用できる。
- 介入はすべて実行時パッチ、`MultiSettingValue` への寄与登録、またはMOD側ファイルへの書き込みに限る。ゲームバイナリとアセットを改変しない。これはSPEC001のEDI連動MODが `GameAssembly.dll` と `global-metadata.dat` のSHA-256を照合するため、共存の必須条件である。
- ゲームは `SiNiSistar2.Obj.StatusCondition.RemainHp1Msv` として `MultiSettingValue<bool>` を持つ。HP0への到達を抑止する機構はゲーム自身のものであり、MODは寄与を1件加えるだけでよい。
- `SiNiSistar2.Obj.LeliaDeadState` は `Live` / `HP0` / `GameOver` の3値であり、ゲームオーバーはHP0とは別の状態である。
- 快楽・絶頂・感度に相当する概念はゲーム側に存在しない。ゲージ、演出、保存はすべてMOD側で構成する。
- ゲームのフラグ変数システム（`SaveFlagAndVariable`）は `VariableType` の固定カテゴリと起動時に確定する固定長配列で構成され、新しい変数名を追加する手段がない。未使用インデックスの占有は、未使用であることを確認する手段がなく、ゲーム更新で進行状態を破壊し得るため採用しない（DEC-206）。
- `SiNiSistar2.Manager.SaveData` は `OnAfterLoadMainSaveData`、`OnBeforeSaveMainSaveData`、`OnAfterSaveMainSaveData` を UniRx `Subject` として公開する。`MainSaveData` は `SelectID` と `LoadedFileName` を持つ。MODはこれらを購読・参照して随伴ファイルを同期できる。
- `AbnormalType.BreastSuper` は実在し、ローカライズ（`ID_Ab_BreastSuper`）とセリフ（`Talk_BreastSuper_01`）を持つ完成済みの状態異常である。イベント限定であるのはコンテンツ側の都合であり、API上の制約ではない。
- 「性的な攻撃」の判別基準はゲーム側に存在しない。攻撃が付与する状態異常（`DamageParameter.m_AbnormalTypes`）からの推論であり、設定で上書きできる形にする。
- 拘束中の攻撃の一部は、ゲージ上の被弾として観測できない可能性がある。快楽の上昇契機は実測で確認する（付録A）。

## 3. 用語

| 用語 | 定義 |
|---|---|
| 快楽 | 拘束中に性的な攻撃を受けることで上昇する `0` 以上 `1` 以下のMOD側の値。上限に達すると絶頂する。 |
| 絶頂 | 快楽が上限へ達したときに発生する一回性のイベント。快楽を `0` へ戻し、絶頂回数を1増やし、感度を増やし、画面演出を再生する。 |
| 絶頂回数 | 直近のリセット以降に発生した絶頂の回数。セーブスロットごとに保存する。 |
| 絶頂限界 | 拘束によるゲームオーバーが成立する絶頂回数。プレイヤーのステータスから算出する。 |
| 感度 | 受けた性的な攻撃と絶頂によって蓄積する `0` 以上のMOD側の値。単調非減少であり、いかなる操作でも減少しない。快楽の上昇量に乗じる。 |
| 性的攻撃 | 快楽を上昇させる攻撃。判別は5.3による。 |
| 非性的攻撃 | 快楽を上昇させない攻撃。捕食、暴力、罠などが該当する。 |
| 随伴ファイル | 感度と絶頂回数を保存するMOD側のファイル。ゲームのセーブスロット1つにつき1つ対応する。 |
| スロットキー | 随伴ファイルとセーブスロットの対応に用いる識別子。`MainSaveData.SelectID` と `LoadedFileName` から構成する。 |
| 絶頂演出 | 絶頂の発生をプレイヤーへ示すMOD側の画面表示。ゲームの演出資産を使用しない。 |
| HP0抑止 | 拘束中にHPが `1` を下回らないようにする制御。`StatusCondition.RemainHp1Msv` への寄与で実現する。 |

## 4. 採用設計

### 4.1 全体構成

```
BepInEx (IL2CPP)
  └ community.sinisistar2.pleasure          ← 本MOD
      ├ PleasurePlugin (BasePlugin)          設定読込、パッチ適用、巻き戻し
      ├ Harmonyパッチ群                      ゲームへの介入点
      ├ PleasureOverlay (MonoBehaviour)      快楽ゲージと絶頂演出の描画
      ├ EnemyCatalogEditor                   敵別分類のゲーム内編集画面（F10）
      └ SiNiSistar2.Pleasure.Core            純粋ロジック（ゲーム参照なし・単体テスト対象）
          ├ PleasureProfile                  設定の検証済み表現
          ├ SexualAttackClassifier           攻撃の性的／非性的判別
          ├ EnemyAttackCatalog               敵別分類の保持と編集
          ├ EnemyAttackCatalogStore          分類カタログの読み書きと検証
          ├ PleasureMeter                    快楽の上昇・減衰・上限到達
          ├ ClimaxLedger                     絶頂回数、絶頂限界、リセット
          ├ SensitivityTrack                 感度の単調蓄積
          └ SidecarDocument                  随伴ファイルの読み書きと検証

  └ community.sinisistar2.difficulty        ← SPEC002（本MODは依存しない）
  └ community.sinisistar2.edi               ← SPEC001（本MODは依存しない）
```

`Core` はUnityにもIL2CPPにも依存しない。快楽の増減、絶頂の判定、限界の算出、感度の蓄積、随伴ファイルの直列化はすべて `Core` に置き、時刻を注入して単体テストで検証する。`Plugin` はゲームの値を読み、`Core` へ渡し、返った決定を適用するだけの層とする。SPEC001・SPEC002 と同じ構成である。

3つのMODは相互に参照せず、BepInExの依存宣言も行わない。いずれか1つだけを配置した構成も正当である。

### 4.2 拘束中の敗北条件の置き換え

```
【MOD導入前】
  拘束 ──> HPダメージ ──> HP0 ──> ゲームオーバー

【MOD導入後】
  拘束 ─┬─> HPダメージ ──> HP1で停止（RemainHp1寄与）──> ゲームオーバーにならない
        │
        └─> 性的攻撃か？
              ├─ いいえ（捕食・暴力）──> 快楽は動かない
              └─ はい ──> 快楽 +（基準量 × 感度係数）
                            │
                            └─> 快楽が上限 ──> 絶頂
                                                ├─ 快楽を0へ
                                                ├─ 絶頂回数 +1
                                                ├─ 感度 +（増分）
                                                ├─ 演出
                                                └─> 絶頂回数 ≧ 絶頂限界 ──> ゲームオーバー
```

HPダメージそのものは止めない。止めるのは「0へ達すること」だけである。ダメージ、ダメージ表示、被弾演出、スリップダメージは従来どおり発生し、拘束から脱出した後は通常どおりHPが枯渇し得る。拘束中だけ死なないという性質は、脱出後の緊張を保つために必要である（DEC-201）。

### 4.3 3つの値の関係

| 値 | 範囲 | 増加 | 減少 | 保存 |
|---|---|---|---|---|
| 快楽 | `0`〜`1` | 拘束中の性的攻撃 | 拘束外で時間減衰、絶頂で `0` | しない（セッション内） |
| 絶頂回数 | `0` 以上の整数 | 絶頂 | セーブポイントのリセットのみ | する |
| 感度 | `0` 以上 | 絶頂、性的攻撃の被弾 | **しない** | する |

快楽は保存しない。拘束から解放された時点の高ぶりを次のセッションへ持ち越す意味がなく、保存すると「ロードした瞬間に絶頂する」状態を作れてしまう。

感度は減少しない。これは要求そのものであり、治療・回復・セーブポイント・リセットのいずれの経路にも減少手段を設けない。感度を下げる方法は、そのセーブを使わないこと以外に存在しない。

### 4.4 永続化

随伴ファイルはゲームのセーブスロットごとに1つ持ち、ゲームのセーブ・ロードに同期して読み書きする。

```
OnAfterLoadMainSaveData  ──> スロットキーを決定 ──> 随伴ファイルを読む ──> 感度・絶頂回数を復元
OnAfterSaveMainSaveData  ──> スロットキーを決定 ──> 随伴ファイルを原子的に書く
```

これにより、ロードし直したときの挙動がゲームのセーブと一致する。セーブ時点へ戻れば絶頂回数も感度もその時点の値へ戻る。感度が「一方通行」であるのは1つのプレイ進行の中での話であり、過去のセーブへ戻る操作まで禁じるものではない。

ゲームのセーブデータへは書き込まない。フラグ変数システムを使う案は、新しい変数名を追加する手段がなく、未使用インデックスの占有がゲーム更新で進行状態を破壊し得るため採用しない（DEC-206）。

### 4.5 代替案とトレードオフ

| 論点 | 採用 | 採用しなかった案 | 理由 |
|---|---|---|---|
| HP0の抑止方法 | `RemainHp1Msv` への寄与 | HPダメージ自体の無効化 | ダメージ表示と演出が消え、拘束が無害に見える。脱出後の残HPも変わってしまう |
| 同上 | `RemainHp1Msv` への寄与 | `HP` の値を毎フレーム書き戻す | ゲーム側の寄与機構を無視することになり、他の `RemainHp1` 提供元と競合する |
| 敗北条件 | 絶頂回数の限界 | 快楽ゲージ自体を敗北条件にする | 1回の拘束で決着し、「何度も絶頂させられた末に」という蓄積が表現できない |
| 性的攻撃の判別 | 付与する状態異常からの自動判別＋設定上書き | 敵IDの列挙のみ | 全敵を調べる工数が大きく、漏れた敵が無反応になる。自動判別なら未知の敵にも既定の答えが出る |
| 絶頂演出 | MOD独自のオーバーレイ | ゲームの演出資産の流用 | 流用可能な資産があるか実機調査が必要で、無ければ成立しない。独自描画は確実に出せる |
| 永続化 | セーブスロット単位の随伴ファイル | ゲームのフラグ変数システム | 新規変数名を追加できず、未使用インデックスの占有はゲーム更新で進行状態を破壊し得る（DEC-206） |
| 同上 | 随伴ファイル | セッション限定（保存しない） | 感度が「一方通行で蓄積する」という要求が実質成立しない |
| 感度の減少 | 手段を設けない | 高難度の治療手段を用意する | 要求が明示的に一方通行を求めている。逃げ道を作ると蓄積の意味が薄れる |

## 5. 制御仕様

### 5.1 拘束中のHP0抑止

- プレイヤーの拘束が成立している間、MODは `StatusCondition.RemainHp1Msv` へMOD固有の寄与キーで `true` を登録する。
- 拘束が終了した時点で寄与を解除する。シーン遷移、巻き戻し、プラグインの `Unload` でも解除する。
- HPダメージ、ダメージ表示、被弾演出、スリップダメージは抑止しない。
- 拘束外のHP0によるゲームオーバーには介入しない。
- 拘束中にHPが1で停止している状態から脱出した場合、HPは1のままとする。回復は行わない。

### 5.2 快楽ゲージ

- 快楽は `0` 以上 `1` 以下のMOD側の値とし、プレイヤーに対してのみ保持する。
- 上昇の条件は次のすべてを満たすことである。
  1. プレイヤーの拘束が成立している。
  2. プレイヤーが受けたダメージが5.3により性的攻撃と判定される。
- 1回の被弾による上昇量は `PleasureGainPerHit × (1 + 感度 × SensitivityGainScale)` とする。
- 拘束が成立していない間、快楽は毎秒 `PleasureDecayPerSecond` の割合で減少し、`0` を下限とする。拘束中は減衰しない。
- 快楽はセーブしない。ゲームの起動ごとに `0` から始まる。
- 快楽が `1` に達した時点で5.4の絶頂が発生する。

### 5.3 性的攻撃の判別

敵ごとの宣言は、設定ファイルとは別の**分類カタログ**（6.2）に持つ。宣言は `Auto`・`Sexual`・`NonSexual` の3値であり、既定はすべて `Auto` である。

判別は次の順で行い、最初に一致した規則を採用する。

1. 拘束相手の `GalleryEnemyID` の宣言が `NonSexual` の場合、非性的攻撃とする。
2. 拘束相手の `GalleryEnemyID` の宣言が `Sexual` の場合、性的攻撃とする。
3. 受けたダメージの `DamageParameter.m_AbnormalTypes` が `SexualAbnormalTypes` のいずれかを含む場合、性的攻撃とする。
4. いずれにも当たらない場合、非性的攻撃とする。

- 宣言が `Auto` の敵は規則1と2に当たらず、規則3の状態異常判定へ進む。すなわち `Auto` は「ゲーム側の情報に任せる」であって「非性的」ではない。
- 既定は「判別できなければ非性的」とする。捕食・暴力を行う敵で快楽が上昇することは、要求に対する明確な誤りであり、無反応より重い。
- 拘束相手を特定できない場合、規則1と2を適用せず規則3へ進む。
- 判別結果は被弾ごとに評価する。同じ敵でも攻撃によって性的・非性的が分かれることを許す。
- 分類器はカタログを参照で保持する。編集は次の被弾から効き、再起動を要しない（DEC-212）。

### 5.4 絶頂

- 快楽が `1` に達したとき、MODは次を1回だけ行う。
  1. 快楽を `0` へ戻す。
  2. 絶頂回数を1増やす。
  3. 感度を `SensitivityPerClimax` だけ増やす。
  4. 絶頂演出を開始する。
  5. 5.5の限界判定を行う。
- 絶頂演出の間、快楽は上昇しない。演出の長さは `ClimaxOverlaySeconds` とする。
- 絶頂演出はMODが描画する。ゲームの演出資産、アニメーション、スプライトを使用しない。
- 絶頂演出はゲームの進行を停止させない。`Time.timeScale` を変更しない。
- 絶頂演出が描画できない場合でも、1から3および5.5の判定は行う。演出の欠落を理由に絶頂を成立させないことはしない。

### 5.5 絶頂限界とゲームオーバー

- 絶頂限界 `N` は `ClimaxLimitBase + floor(耐久の最大値 × ClimaxLimitPerDurability)` とする。
- 耐久の最大値は `PlayerStatusManager.m_MaxDurability` から読む。読めない場合は `ClimaxLimitBase` のみとし、その旨を記録する。
- 絶頂回数が `N` 以上に達した時点で、拘束によるゲームオーバーを成立させる。
- 発火は、5.1のHP0抑止の寄与を解除することで行う。抑止が外れた拘束中のHPは通常どおり `0` へ達し、ゲームの既存の死亡・ゲームオーバー経路がそのまま走る。MODはゲームオーバー処理そのものを呼ばない。
- この方式により、リトライ、ペナルティ、演出、およびSPEC001の `game-over` トリガーが従来どおり成立する。敗北の**原因**は絶頂回数であり、HPはその結果を送り届ける手段にすぎない。
- 寄与を解除した後、絶頂回数が `N` 未満へ戻るまで再登録しない。
- HPが `0` へ達する前に拘束から脱出した場合、脱出時点で寄与の解除状態を維持する。次に拘束されたときも、絶頂回数が `N` 以上である限り抑止しない。
- 絶頂回数が `N` 未満の間、絶頂はゲームオーバーを引き起こさない。
- `N` が `0` 以下になる設定は設定エラーとし、この機構を無効化する。

### 5.6 絶頂回数のリセット

- セーブポイントまたはオベリスク（聖なる像）がアクティベートされたとき、絶頂回数を `0` へ戻す。
- 感度は戻さない。
- 検出は `SavePointAsyncLabel.ExecutionOneAsync` の実行とする。当該ラベルの `IsObeliskLabel` により、オベリスクか通常のセーブポイントかを区別できる。
- `ResetAtObeliskOnly` が真の場合、`IsObeliskLabel` が真のときだけリセットする。偽の場合はいずれでもリセットする。既定は偽とする。`SavePointSelector.m_ChangeObeliskInHardMode` により、難易度によってはセーブポイントがオベリスクへ置き換わるため、オベリスク限定にすると難易度によってリセット地点が消える構成があり得る。
- セーブの完了を契機としない。オートセーブでリセットされることを避けるためであり、`MainSaveData.IsAutoSave` による区別に依存しないためでもある。
- リセットは随伴ファイルへ反映する。

### 5.7 感度

- 感度は `0` 以上のMOD側の値とし、単調非減少とする。
- 増加の契機は次の2つである。
  1. 絶頂したとき、`SensitivityPerClimax` だけ増やす。
  2. 拘束中に性的攻撃を受けたとき、`SensitivityPerSexualHit` だけ増やす。
- MODは感度を減少させる経路を持たない。状態異常の治療、回復、休息、セーブポイントのアクティベート、絶頂回数のリセットのいずれでも減少させない。
- 感度は `SensitivityCap` を上限とする。上限は成長の頭打ちであり、減少ではない。
- 感度は快楽の上昇量に5.2の式で作用する。

### 5.8 通常プレイ中の `BreastSuper`

`BreastSuper` は完成済みの状態異常であり、イベント限定であるのはコンテンツ側の都合である。MODはこれを **`Breast` の上位ペナルティ**として通常プレイへ開放する。

**遷移の条件。** `Breast` が既に最大レベルにある状態で、さらに `Breast` の付与が `BreastSuperAfterApplications` 回到達したとき、`BreastSuper` へ遷移する。

- **最大レベル未満の付与は数えない。** その付与にはまだ行き先があり、レベルを上げることがゲーム自身の段階表現だからである。数えてしまうと、通常の進行が終わらないうちに上限側の演出が来る。最大レベルの `Breast` が吸収できなくなった打撃だけが、`Breast` の表現を超えた証拠になる。
- `BreastSuperSensitivityThreshold` を満たさない間は回数を保持したまま遷移を保留する。破棄はしない。閾値を満たした次の付与で遷移する。
- 既に `BreastSuper` がある場合は数えず、回数を `0` へ戻す。上位が存在しないため、治療後に再び膨乳したときは初めから積み直す。
- 遷移時、`BreastSuperReplacesBreast` が真なら `Breast` を除去する。`BreastSuper` の付与を先に行い、除去を後に行う。逆順は両方が存在しないフレームを作り、身体表現と立ち絵が状態異常一覧から駆動されるためである。
- 付与はゲームの状態異常付与経路（`AbnormalList.AddAbnormal(AbnormalType, int, DamageStack)`）で行い、`AbnormalList.Has` が真を返す実体のある状態として成立させる。偽の状態を報告させてはならない。`AbnormalSaveData` は `m_Type` と `m_Level` を持つため、ゲーム自身のセーブに乗る。MODの随伴ファイルへは書かない。
- 回数は随伴ファイルへ保存する。ロードで白紙に戻ると、上限付近まで積んだ状態が失われる（FR-222）。
- 付与の出所は問わない。敵の攻撃、アイテムの使用、author されたイベントのいずれも同じく数える。膨乳を付与するアイテムの多重使用で遷移させられることは、デバッグ手段として必要である（FR-244）。
- `BreastSuperAfterApplications` の既定は `0` とし、遷移が起こらない状態で出荷する（FR-233）。
- `BreastSuperCountBelowMaxLevel` を真にすると最大レベル未満の付与も数える。デバッグ専用であり、既定は偽である。

**実測（2026-08-08）。** `Breast` の `MaxLevel` は `1` である。したがって `Breast` は付与された時点で最大レベルにあり、以後の付与はすべて計数対象になる。`BreastSuperAfterApplications = 3` は「膨乳した状態でさらに3回」を意味する。

**治療。** MODは治療手段を新設しない。ゲームが持つ治療経路が `BreastSuper` へ届くかどうかを実測し（付録A A-14）、届かない場合にのみ最小の介入を行う。

- 静的調査で確認できたのは次である。`Breast` の治療は `ChoiceLocalizeID.ID_Choice_CureBreast` の選択肢から `DramaID.ID_CureBreast_Player`（自身で搾乳）と `ID_CureBreast_Haanja1` / `ID_CureBreast_Haanja2`（ハーニャによる搾乳）へ分岐する。搾乳の映像は `SceneID.Ga_Player_Other_1_Breast2` として存在する。
- 治療イベントを出す条件がどの値を見ているかは、シーンとプレハブに author されたデータ側にあり、静的には読めない。`OneConditionCheckType` には `AbnormalCondition`（型とレベルの一致）と `PhysicalCondition`（`PhysicalConditionFlag` の一致）と `HasHaanjaCureableStatus` があり、どれで書かれているかで結論が変わる。
- `BreastSuper` が `PhysicalConditionFlag.Breast` を持ち、治療イベントが `PhysicalCondition` で書かれている場合、**MODの介入なしに既存の治療がそのまま届く**。これが最良の結末であり、最初に確認する。
- 届かない場合の最小の介入は `AbnormalData.m_HaanjaCanCure` を真にすることである（`BreastSuperMakeHaanjaCurable`）。これはゲーム自身の治療イベントに「この状態異常も対象である」と伝えるだけで、治療処理そのものはゲームが行う。セッション限りの変更とし、アンロード時に戻す。
- **MODから搾乳シーンを再生する案は採らない。** `GaTakePlayer.Play` と `DramaManager.PlayDrama` は公開されているが、前者はギャラリー用のシーンと `GaTakePlayer` の実体が読み込まれていることを要求し、フィールド上で満たせない。ギャラリーモードへ入れる案は `GalleryManager.IsActive` が `LabelEvent.DisableWhenGallery` をはじめ広範な挙動を切り替えるため、進行中のランを壊す危険が実利を上回る。後者は台詞だけを再生するもので、映像を伴わない。詳細は DEC-213。

### 5.9 永続化と同期

- 随伴ファイルはスロットキーごとに1つとし、感度、絶頂回数、書き込み時点のゲームビルド識別子、スキーマ版を保持する。
- スロットキーは `MainSaveData.SelectID` と `LoadedFileName` から構成する。両方を保持し、照合に用いる。
- `OnAfterLoadMainSaveData` で随伴ファイルを読み、感度と絶頂回数を復元する。対応するファイルがない場合は初期値（感度 `0`、絶頂回数 `0`）とする。
- `OnAfterSaveMainSaveData` で随伴ファイルを原子的に書く。部分書き込みを残さない。
- スキーマ版が非対応の随伴ファイルは読まず、初期値で開始し、既存ファイルを上書きしない。
- ゲームビルド識別子が異なる随伴ファイルは、値を読んだうえで不一致を記録する。値の破棄は行わない。
- 随伴ファイルの読み書きに失敗しても、ゲームと本MODの他の機構は継続する。

### 5.10 巻き戻し

- プラグインの `Unload`、シーン遷移、例外による中断のいずれでも、`RemainHp1Msv` への寄与を解除する。
- 解除に失敗した介入は、対象と理由を記録する。
- 全機構を無効化した設定で起動した場合、Harmonyパッチを適用せず、随伴ファイルも書かない。

### 5.11 分類カタログの編集画面

拘束中の敵が性的攻撃を行うかどうかは、画面に映っているものを見て下す判断である。その判断ができる唯一の場所はその敵の前であり、セッションの合間に記憶を頼りにテキストを編集する形では正しく下せない。したがってMODはゲーム内に編集画面を持つ。

- `F10` で開閉する。ゲームプレイ中、拘束中、タイトル画面のいずれでも開ける。
- 一覧はゲームが定義する `GalleryEnemyID` の全件を対象とし、**拘束された経験のある敵を先頭**に、その中は識別子順に並べる。
- 上下キーとホイールで移動し、`Space`・左右キー・クリックで `Auto` → `Sexual` → `NonSexual` → `Auto` と巡回する。
- `Tab` は「拘束された経験のある敵のみ」と「全件」を切り替える。
- `Enter` で保存して閉じる。`Escape` は開いた時点の内容へ戻して閉じる。
- 現在プレイヤーを拘束している敵を画面上部に表示し、開いた時点でその行を選択する。
- 描画は本MODのオーバーレイと同じ即時モードGUIで行い、ゲームのUI階層へオブジェクトを追加しない。`Time.timeScale` を変更せず、ゲームを停止させない。

編集画面はゲームの入力を奪わない。IMGUIのイベント消費はIMGUIの内部にとどまり、ゲームは独自に入力を読むため、画面を開いている間のクリックやキー入力はゲーム側にも届く。したがって拘束中に開く場合は、キーボード操作のみで完結できる経路（上下キーと `Space`）を用意する。

## 6. 設定

BepInEx の `ConfigEntry` として `BepInEx/config/community.sinisistar2.pleasure.cfg` に生成する。

| セクション | キー | 型 | 既定値 | 意味 |
|---|---|---|---|---|
| `General` | `Enabled` | bool | `true` | 全機構の有効・無効。 |
| `Survival` | `SuppressHp0WhileBound` | bool | `true` | 拘束中にHPが0へ達することを抑止するか。 |
| `Pleasure` | `PleasureGainPerHit` | float | 実測後に確定 | 性的攻撃1回あたりの快楽上昇量。 |
| `Pleasure` | `PleasureDecayPerSecond` | float | 実測後に確定 | 拘束外での毎秒の減衰量。 |
| `Pleasure` | `SexualAbnormalTypes` | string | 6.1参照 | 性的攻撃と判定する状態異常名のカンマ区切り。 |
| `Pleasure` | `SexualEnemyIds` | string | 6.2参照 | **種として使うのみ。** 分類カタログを新規作成するときに `Sexual` として書き込む `GalleryEnemyID` のカンマ区切り。カタログが存在する場合は参照しない。 |
| `Pleasure` | `NonSexualEnemyIds` | string | 空 | 種として使うのみ。新規作成時に `NonSexual` として書き込む `GalleryEnemyID`。 |
| `Climax` | `ClimaxOverlaySeconds` | float | `1.5` | 絶頂演出の長さ。 |
| `Climax` | `ClimaxLimitBase` | int | 実測後に確定 | 絶頂限界の基礎値。 |
| `Climax` | `ClimaxLimitPerDurability` | float | 実測後に確定 | 耐久の最大値1あたりの限界増分。 |
| `Climax` | `EnableClimaxGameOver` | bool | `false` | 限界到達でHP0抑止を解除するか。実機確認まで `false`。 |
| `Climax` | `ResetAtObeliskOnly` | bool | `false` | 真ならオベリスクでのみ絶頂回数をリセットする。偽なら通常のセーブポイントでもリセットする。 |
| `Sensitivity` | `SensitivityPerClimax` | float | 実測後に確定 | 絶頂1回あたりの感度増加。 |
| `Sensitivity` | `SensitivityPerSexualHit` | float | 実測後に確定 | 性的攻撃1回あたりの感度増加。 |
| `Sensitivity` | `SensitivityGainScale` | float | 実測後に確定 | 感度が快楽上昇量へ与える係数。 |
| `Sensitivity` | `SensitivityCap` | float | `10.0` | 感度の上限。 |
| `BreastSuper` | `BreastSuperAfterApplications` | int | `0` | 最大レベルの `Breast` へさらに何回付与されたら `BreastSuper` へ遷移するか。`0` は遷移しない。 |
| `BreastSuper` | `BreastSuperSensitivityThreshold` | float | `0` | 遷移に必要な感度。`0` は要求しない。 |
| `BreastSuper` | `BreastSuperReplacesBreast` | bool | `true` | 遷移時に `Breast` を除去するか。 |
| `BreastSuper` | `MakeHaanjaCurable` | bool | `false` | `BreastSuper` をハーニャの治療対象として印付けするか。付録A A-14 の実測まで無効。 |
| `BreastSuper` | `CountBelowMaxLevel` | bool | `false` | 最大レベル未満の付与も数えるか。デバッグ専用。 |
| `Diagnostics` | `LogTransitions` | bool | `false` | 快楽・絶頂・感度の変化をログへ記録するか。 |

「実測後に確定」と記した既定値は、付録Aの実測を経て確定する。実測前は挙動を変更しない値（上昇量0、増分0）とし、MODを導入しただけではゲームの挙動が変わらないようにする。ただし `SuppressHp0WhileBound` は既定で有効とする。これは撤廃そのものが要求であり、調整値を持たないためである。

### 6.1 性的とみなす状態異常の既定集合

`AbnormalType` の列挙子名からの推論であり、ゲーム側にこの分類は存在しない。設定で変更できる。

`Lustfull`、`Lustfull_Forever`、`LustMarkCurse`、`Defilement`、`Semen`、`Semen_mucus`、`Pregnant`、`Pregnant_Demi`、`Breast`、`BreastSuper`、`Milk`、`WetNurse`、`MindControl`、`MindIntegration`、`Infertility`、`InfertilityBlessing`

SPEC002 の快楽系集合とは目的が異なるため一致させない。SPEC002 は「抵抗の意思が損なわれる状態」を、本仕様は「性的な行為を受けたことを示す状態」を指す。`Defilement` は SPEC002 では意図的に除外されるが、本仕様では性的攻撃の指標として含める。

### 6.2 敵別分類カタログ

敵ごとの宣言は `BepInEx/config/community.sinisistar2.pleasure/enemy-attacks.json` に持つ。`ConfigEntry` のカンマ区切り文字列ではなくファイルとする理由は3つある。対象が108件あり1行に収まらないこと、判断が敵ごとに独立していて差分として読めるべきこと、そしてゲーム内の編集画面が書き戻す先を必要とすることである。

```json
{
  "schemaVersion": 1,
  "note": "...",
  "enemies": [
    { "id": "GaID_PictureFrameBig", "kind": "Sexual", "seen": true, "note": "..." },
    { "id": "GaID_MeatWormVore", "kind": "Auto", "seen": false }
  ]
}
```

| フィールド | 意味 |
|---|---|
| `id` | `GalleryEnemyID` の列挙子名。 |
| `kind` | `Auto`・`Sexual`・`NonSexual`。認識できない語は `Auto` として扱う。 |
| `seen` | この敵に拘束された経験があるか。編集画面の並び順にのみ使う。判別には影響しない。 |
| `note` | 由来の記録。MODは読み書きするだけで解釈しない。 |

- 起動時にファイルが無ければ、`SexualEnemyIds` / `NonSexualEnemyIds` を種として作成する。既存の設定が更新で失われないようにするためである。
- 起動時に、ゲームが定義する `GalleryEnemyID` のうちファイルに無いものを `Auto` として追加する。遭遇前の敵を分類できるようにするためである。
- 書き込みは原子的に行う。読めないファイルと非対応スキーマ版のファイルは読まず、上書きもしない。破損したファイルは退避して新規から開始する。随伴ファイル（5.9）と同じ規律とする。
- 書き込みの契機は、初回の拘束、編集画面の保存、プラグインの終了の3つとする。被弾ごとに書かない。

## 7. 他MODとの互換性

### 7.1 破ってはならない前提

| 対象 | 制約 | 理由 |
|---|---|---|
| `GameAssembly.dll`、`global-metadata.dat` | 改変しない | SPEC001がSHA-256を照合し、不一致ならデバイス出力を無効化する |
| `Time.timeScale` | 変更しない | SPEC001がポーズ検出に使用する |
| `Animator` の状態、再生クリップ、`GalleryEnemyID`、take名 | 変更しない | SPEC001がトリガー同定に使用する |
| `Lelia.IsHold`、`Bind.BinderEnemy` | 意味を変えない | SPEC001が `hold` トリガー判定に使用する |
| `AbnormalList.Has` | 偽の状態異常を報告させない | SPEC001がfiller選択に使用する。5.8の `BreastSuper` は実体のある付与であり、これに反しない |
| `GachaGachaSystem.CurrentValue`、`GachaGachaParameter` | 触れない | SPEC002の無力化窓が管理する |
| `PlayerStatusManager.MoveSlowRateMsv` | 触れない | SPEC002の復帰遅延窓が寄与を登録する |
| `PlayerStatusManager.IsHardMode`、`s_GameDifficultyForCheck` | 触れない | SPEC002が管理する |
| ゲームのセーブデータ、`SaveFlagAndVariable` | 書き込まない | 2.2、DEC-206 |

### 7.2 許容される観測可能な差分

- **`hold` トリガーの継続時間の延長** — 拘束中にHPで決着しなくなるため、SPEC001の `hold` トリガーが有効な時間が延びる。トリガーの同一性は変わらない。
- **`BreastSuper` の新規出現** — 5.8により通常プレイで発生し得るようになる。SPEC001はこれを状態異常として観測し、`mappings.json` に該当ルールがなければ既定fillerを選ぶ。必要なら利用者がルールを追加する。
- **`game-over` トリガーの遅延** — 拘束中のゲームオーバーは、HPが尽きた時点ではなく絶頂限界に達した後に起きる。5.5はHP0抑止の解除によって発火するためHP0を経由し、SPEC001の `game-over` トリガーは従来どおり `Lelia.IsHP0` で成立する。トリガーの同一性と発生条件は変わらず、発生の時期だけが後ろへ動く。

### 7.3 SPEC002 との相互作用

両MODは拘束中に同時に作用する。設計上の干渉はないが、体感は重なる。

- SPEC002の無力化窓は脱出を難しくし、本MODは拘束の継続時間を延ばす。両方が有効な構成では拘束が大幅に長引く。
- SPEC002の復帰遅延窓は脱出直後の再拘束を招き、本MODでは再拘束が快楽の再上昇を意味する。
- 3つのMODは相互に依存せず、ロード順序を仮定しない。いずれか1つだけの構成も正当である。

### 7.4 寄与キーの分離

本MODとSPEC002はいずれも `MultiSettingValue` へ寄与を登録する。対象は異なる（本MODは `RemainHp1Msv`、SPEC002は `MoveSlowRateMsv`）が、同一の値へ登録する将来の変更に備え、寄与キーはプラグインGUIDで前置する。

## 8. 機能要件

| ID | 要件 | 優先度 | 根拠 |
|---|---|---|---|
| FR-201 | MODはBepInEx IL2CPPプラグインとしてロードされ、GUID `community.sinisistar2.pleasure` を持たなければならない。 | Must | 他MODとの識別 |
| FR-202 | MODは `GameAssembly.dll`、`global-metadata.dat`、ゲームアセット、ゲームのセーブデータ、`SaveFlagAndVariable` を書き換えてはならない。 | Must | 共存条件（7.1）、DEC-206 |
| FR-203 | プレイヤーの拘束が成立している間、MODは `StatusCondition.RemainHp1Msv` へ寄与を登録し、HPが `1` を下回らないようにしなければならない。 | Must | 拘束中のHP0撤廃（1.2） |
| FR-204 | MODは拘束の終了、シーン遷移、巻き戻しのいずれでも `RemainHp1Msv` への寄与を解除しなければならない。 | Must | 拘束外での不死化の防止 |
| FR-205 | MODは拘束中のHPダメージ、ダメージ表示、被弾演出、スリップダメージを抑止してはならない。 | Must | 拘束が無害に見えることの防止（DEC-201） |
| FR-206 | MODは拘束外のHP0によるゲームオーバーに介入してはならない。 | Must | 既存の敗北条件の保存 |
| FR-207 | MODは、拘束中かつ性的攻撃と判定された被弾のときにだけ快楽を上昇させなければならない。 | Must | 敵の性質による区別（1.2） |
| FR-208 | MODは5.3の順序で性的攻撃を判別しなければならない。判別できない場合は非性的として扱わなければならない。 | Must | 捕食・暴力での誤上昇の防止 |
| FR-209 | 快楽の上昇量は感度に応じて増加しなければならない。 | Must | 感度が絶頂しやすさへ作用する（1.2） |
| FR-210 | 快楽は拘束外で減衰し、`0` を下限としなければならない。拘束中は減衰させてはならない。 | Must | 拘束中の緊張の維持 |
| FR-211 | 快楽が上限に達したとき、MODは快楽を `0` へ戻し、絶頂回数を1増やし、感度を増やし、絶頂演出を開始しなければならない。これらは1回の到達につき1回だけ行わなければならない。 | Must | 絶頂の定義（5.4） |
| FR-212 | 絶頂演出はMODが描画しなければならない。ゲームの演出資産を使用してはならず、`Time.timeScale` を変更してはならない。 | Must | 資産依存の排除、SPEC001のポーズ検出の保護 |
| FR-213 | 絶頂演出を描画できない場合でも、MODは絶頂の状態変化と限界判定を行わなければならない。 | Must | 演出の欠落で機構が止まらないこと |
| FR-214 | 絶頂限界は `ClimaxLimitBase` と耐久の最大値から算出しなければならない。耐久を読めない場合は基礎値のみとし、その旨を記録しなければならない。 | Must | ステータスによる天井（1.2） |
| FR-215 | 絶頂回数が絶頂限界以上に達したとき、MODは5.1のHP0抑止の寄与を解除し、ゲームの既存の死亡経路によってゲームオーバーを成立させなければならない。MODはゲームオーバー処理そのものを呼んではならない。 | Must | 敗北条件の置き換え（1.2）、既存の復帰処理の保存（DEC-209） |
| FR-216 | 絶頂回数が絶頂限界以上である間、MODはHP0抑止の寄与を再登録してはならない。 | Must | 限界到達後の不死化の防止 |
| FR-217 | `SavePointAsyncLabel.ExecutionOneAsync` の実行を検出したとき、MODは絶頂回数を `0` へ戻さなければならない。感度を戻してはならない。`ResetAtObeliskOnly` が真の場合は `IsObeliskLabel` が真のときだけ戻さなければならない。 | Must | リセット契機（1.2）、5.6 |
| FR-218 | 感度は絶頂と性的攻撃の被弾で増加しなければならない。 | Must | 感度の蓄積（1.2） |
| FR-219 | MODは感度を減少させる経路を持ってはならない。状態異常の治療、回復、セーブポイント、絶頂回数のリセットのいずれでも減少させてはならない。 | Must | 一方通行の要求（1.2） |
| FR-220 | 感度は `SensitivityCap` を上限としなければならない。上限への到達は減少であってはならない。 | Must | 際限のない増加の防止 |
| FR-221 | MODは5.8の条件成立時に `BreastSuper` へ遷移させなければならない。付与はゲームの状態異常付与経路で行い、`AbnormalList.Has` が偽を返す状態を作ってはならない。 | Must | 上位ペナルティ（1.2）、SPEC001のfiller選択の保護 |
| FR-240 | 遷移の計数は、`Breast` が最大レベルにある状態での付与のみを対象としなければならない。最大レベルを読めない場合、遷移を行ってはならない。 | Must | 通常進行との二重計上の防止（5.8） |
| FR-241 | MODは `BreastSuper` の治療手段を新設してはならない。ゲームの既存の治療経路が届くようにする以上の介入を行ってはならない。 | Must | 治療不能状態の防止、ゲーム進行の保存（DEC-213） |
| FR-242 | `AbnormalData` の値を変更する場合、MODは変更前の値を記録し、アンロード時に戻さなければならない。 | Must | 巻き戻し（5.10） |
| FR-244 | 遷移の計数は `Breast` の付与経路を問わず成立しなければならない。敵の攻撃、アイテムの使用、author されたイベントのいずれも同じく数える。同一の付与が複数の経路で観測された場合、二重に数えてはならない。 | Must | アイテムによる検証手段の確保（5.8） |
| FR-245 | MODは `BreastSuper` を適用する前に、ゲームへその読み込みを要求しなければならない。 | Must | 未読み込みの状態異常は付与できない |
| FR-222 | MODは感度と絶頂回数をセーブスロット単位の随伴ファイルへ保存し、ゲームのセーブ・ロードに同期しなければならない。 | Must | 永続化（1.2）、DEC-206 |
| FR-223 | 随伴ファイルの書き込みは原子的でなければならず、部分書き込みを残してはならない。 | Must | 破損データの防止 |
| FR-224 | 対応する随伴ファイルがない場合、MODは初期値で開始しなければならない。 | Must | 新規プレイと既存セーブの受け入れ |
| FR-225 | スキーマ版が非対応の随伴ファイルを、MODは読んではならず、上書きしてもならない。 | Must | 版の食い違いによるデータ喪失の防止 |
| FR-226 | 随伴ファイルの読み書きに失敗しても、MODはゲームと他の機構を継続させなければならない。 | Must | 障害分離 |
| FR-227 | MODは他のMODへのBepInEx依存を宣言してはならず、ロード順序を仮定してはならない。 | Must | 単独構成の許容（7.3） |
| FR-228 | MODは `Time.timeScale`、`Animator` の状態と再生クリップ、`GalleryEnemyID`、take名、`Lelia.IsHold` / `Bind.BinderEnemy` の意味を変更してはならない。 | Must | SPEC001のトリガー同定の保護（7.1） |
| FR-229 | MODは `GachaGachaSystem`、`GachaGachaParameter`、`MoveSlowRateMsv`、`IsHardMode`、`s_GameDifficultyForCheck` へ介入してはならない。 | Must | SPEC002の管理範囲の保護（7.1） |
| FR-230 | MODの介入はメインスレッド上で完結しなければならず、フレーム処理を待機によって停止させてはならない。 | Must | ゲーム性能 |
| FR-231 | MODは起動時に、有効な機構、確定済み調整値、随伴ファイルの位置、適用したパッチ数を記録しなければならない。 | Must | 構成の診断 |
| FR-232 | 快楽の増減、絶頂の判定、限界の算出、感度の蓄積、随伴ファイルの直列化は、ゲームへ依存しない層に置き、単体テストで検証可能でなければならない。 | Must | 実機でしか試せない範囲の縮小（4.1） |
| FR-233 | 実測で確定していない調整値の既定値は、挙動を変更しない値でなければならない。`SuppressHp0WhileBound` は例外とし、既定で有効とする。 | Must | 未検証の値でゲームを壊さない |
| FR-234 | `Enabled` が偽の場合、MODはHarmonyパッチを適用してはならず、随伴ファイルを書いてはならない。 | Must | 完全な無効化の保証 |
| FR-235 | MODは敵別の性的・非性的の宣言を、設定ファイルとは独立した分類カタログファイルに保持しなければならない。カタログが存在しない場合、既存の `SexualEnemyIds` / `NonSexualEnemyIds` を種として作成しなければならない。 | Must | 外部ファイル化（5.3、6.2）、更新時の設定喪失の防止 |
| FR-236 | MODはゲーム内から呼び出せる編集画面を提供し、敵ごとに `Auto`・`Sexual`・`NonSexual` を切り替えられなければならない。変更は再起動を要さず、次の被弾の判別から反映されなければならない。 | Must | 判断を対象の前で下せること（5.11） |
| FR-237 | 編集画面はゲームが定義する `GalleryEnemyID` の全件を列挙し、拘束された経験のある敵を先頭に並べなければならない。 | Must | 遭遇前の分類（5.11） |
| FR-238 | 編集画面は保存と取り消しを区別しなければならない。取り消したとき、開いた時点の宣言へ戻さなければならない。 | Must | 誤操作からの復帰 |
| FR-239 | 分類カタログファイルの書き込みは原子的でなければならず、非対応スキーマ版のファイルを読んでも上書きしてもならない。読み書きに失敗しても、MODはゲームと他の機構を継続させなければならない。 | Must | 手作業の判断の保護、障害分離 |

## 9. エラーと復旧

| 条件 | 動作 | 復旧 |
|---|---|---|
| ゲームビルドが対象と一致しない | 全機構を無効化し、期待するSHA-256と実際の値を提示 | 対象ビルドを配置するか対応版へ更新 |
| Harmonyパッチの対象が見つからない | 当該機構だけを無効化し、見つからなかったシグネチャを提示 | ゲーム更新に追随したパッチ対象の更新 |
| `RemainHp1Msv` を解決できない | HP0抑止を無効化し、他の機構は継続。拘束中の敗北条件は従来どおりになる旨を提示 | 対応ゲームビルドの確認 |
| `RemainHp1Msv` の寄与解除に失敗 | 対象と理由を記録し、以後この機構を無効化 | 再起動で素の状態へ戻る |
| ゲームオーバーの発火手段が未確定 | 成立条件を検出しても発火せず、検出したことを記録 | 付録A A-4 の確定後に `EnableClimaxGameOver` を有効化 |
| 耐久の最大値を読めない | `ClimaxLimitBase` のみで限界を算出し、その旨を記録 | 対応ゲームビルドの確認 |
| 絶頂限界が `0` 以下 | 当該機構を無効化し、設定値を提示 | 設定の修正 |
| 絶頂演出を描画できない | 状態変化と限界判定は行い、演出の欠落を記録 | 継続 |
| 拘束相手を特定できない | 敵ID規則を適用せず、状態異常による判別へ進む | 継続 |
| 未知の `AbnormalType` 名 | 当該要素を無視し、無視した名前を提示 | 設定の修正 |
| 負の調整値 | 当該機構を無効化し、キー名と値を提示 | 設定の修正 |
| 随伴ファイルが存在しない | 初期値で開始 | 継続。次のセーブで作成される |
| 随伴ファイルが破損・解析不能 | 初期値で開始し、当該ファイルを上書きせず、退避名で保全 | 手動確認 |
| 随伴ファイルのスキーマ版が非対応 | 読まず、上書きせず、初期値で開始。検出した版と要求する版を提示 | 対応版のMODを使用 |
| 随伴ファイルのゲームビルドが異なる | 値を読んだうえで不一致を記録 | 継続 |
| 随伴ファイルの書き込み失敗 | ゲームと他の機構は継続し、失敗を記録 | 書き込み可能になった次のセーブで再試行 |
| スロットキーを決定できない | 随伴ファイルを読み書きせず、セッション内の値のみで動作し、その旨を記録 | ロード完了後に再取得 |
| プラグインの `Unload` | 寄与を解除し、パッチを解除 | 該当なし |

## 10. 非機能要件

### 10.1 性能

- MODの介入はメインスレッド上で完結し、待機によってフレーム処理を停止させない。
- 毎フレームの全 `SiNiObject` 走査を行わない。快楽の評価は被弾時、減衰の評価は拘束外のみとする。
- 随伴ファイルの入出力はセーブ・ロードの契機に限り、毎フレーム行わない。
- 絶頂演出の描画がフレーム処理を遅延させない。

### 10.2 安全性

- ゲームのデータへの書き込みは、寄与登録、状態異常の正規の付与、および読み取りの差し替えに限る。
- 寄与の解除は例外経路でも実行する。
- `MultiSettingValue.AllClear` を呼ばない。
- ゲームのセーブデータへ書き込まない。

### 10.3 互換性

- 他MODとの共存条件は7章に従う。
- ゲーム更新でパッチ対象が変わった場合、当該機構だけを無効化し、他の機構とゲーム本体を継続させる。
- 随伴ファイルはスキーマ版を持ち、非対応版を読まない。

### 10.4 可観測性

- 起動時に、有効な機構、確定済み調整値、随伴ファイルの位置、適用パッチ数を記録する。
- 快楽・絶頂・感度の変化は `LogTransitions` が有効なときに記録する。
- ゲームオーバーの成立条件を検出したが発火しなかった場合、その事実を記録する。

## 11. 受け入れ条件

| ID | 対応要件 | シナリオと期待結果 |
|---|---|---|
| AC-201 | FR-201, FR-202 | Given MODを配置して起動・終了する / When ファイルを確認する / Then `GameAssembly.dll` と `global-metadata.dat` のSHA-256が起動前と一致し、ゲームのセーブデータに差分がない |
| AC-202 | FR-203, FR-205 | Given 拘束されHPが減り続ける状況 / When HPが尽きるまで待つ / Then HPは `1` で止まり、ゲームオーバーにならない。ダメージ表示と被弾演出は従来どおり発生する |
| AC-203 | FR-204 | Given 拘束中でHPが `1` / When 脱出する、またはシーンが遷移する / Then 寄与が解除され、以後の被弾でHPが `0` に達し得る |
| AC-204 | FR-206 | Given 拘束されていない状態でHPが尽きる / When ゲームオーバーになる / Then MOD導入前と同じ挙動である |
| AC-205 | FR-207, FR-208 | Given 性的な攻撃を行う敵に拘束される / Then 快楽が上昇する。Given 捕食・暴力のみを行う敵に拘束される / Then 快楽が上昇しない |
| AC-206 | FR-208 | Given `NonSexualEnemyIds` にある敵 / When 性的な状態異常を伴う攻撃を受ける / Then 快楽が上昇しない（敵ID規則が状態異常規則に優先する） |
| AC-207 | FR-209 | Given 感度が異なる2つの状態 / When 同じ攻撃を受ける / Then 感度が高いほうが快楽の上昇量が大きい |
| AC-208 | FR-210 | Given 快楽が上昇した状態で拘束から脱出する / When 時間が経過する / Then 快楽が減衰し `0` を下回らない。Given 拘束中 / Then 減衰しない |
| AC-209 | FR-211 | Given 快楽が上限に達する / Then 快楽が `0` へ戻り、絶頂回数が1増え、感度が増え、演出が開始する。When 同一フレームで再評価する / Then 二重に加算されない |
| AC-210 | FR-212 | Given 絶頂が発生する / When 演出中のゲーム状態を確認する / Then `Time.timeScale` が変化せず、ゲームの進行が停止しない |
| AC-211 | FR-213 | Given 演出の描画に失敗する状況 / When 絶頂が発生する / Then 絶頂回数と感度は正しく更新され、欠落が記録される |
| AC-212 | FR-214 | Given 耐久の最大値が異なる2つの状態 / When 絶頂限界を確認する / Then 耐久が大きいほうが限界が大きい |
| AC-213 | FR-215 | Given `EnableClimaxGameOver` が真で絶頂回数が限界に達する / When 拘束が続く / Then HP0抑止が解除されHPが `0` へ達し、ゲームの通常のゲームオーバーが発生する。リトライとペナルティが従来どおり動作し、SPEC001の `game-over` トリガーも発生する |
| AC-229 | FR-216 | Given 絶頂回数が限界以上でHPが `0` へ達する前に脱出する / When 再び拘束される / Then HP0抑止が再登録されず、HPが `0` へ達し得る |
| AC-214 | FR-217 | Given 絶頂回数が1以上 / When オベリスクをアクティベートする / Then 絶頂回数が `0` になり、感度は変化しない。Given `ResetAtObeliskOnly` が真 / When 通常のセーブポイントを使う / Then 絶頂回数は変化しない |
| AC-215 | FR-218, FR-219 | Given 感度が蓄積した状態 / When 状態異常を治療し、回復し、セーブポイントをアクティベートする / Then 感度が減少しない |
| AC-216 | FR-220 | Given 感度が上限付近 / When さらに絶頂する / Then 感度が上限を超えず、減少もしない |
| AC-217 | FR-221 | Given `BreastSuperAfterApplications` が `2` で `Breast` が最大レベル / When さらに2回 `Breast` を受ける / Then `BreastSuper` が付与され、`AbnormalList.Has(BreastSuper)` が真を返し、SPEC001が当該状態異常を観測できる |
| AC-235 | FR-240 | Given `Breast` が最大レベル未満 / When 繰り返し `Breast` を受ける / Then レベルのみが上がり遷移しない。Given 最大レベル到達後 / Then そこからの付与が数えられる |
| AC-236 | FR-221, FR-222 | Given 遷移の手前まで計数が進んだ状態でセーブする / When 再起動して同じスロットをロードする / Then 計数が保存時点から続く |
| AC-238 | FR-244 | Given `BreastSuperAfterApplications` が `3` / When 膨乳を付与するアイテムを3回使う / Then `BreastSuper` へ遷移する。ログの計数が1回の使用につき1だけ進む |
| AC-237 | FR-241, FR-242 | Given `MakeHaanjaCurable` を有効にする / When ハーニャの治療を受ける / Then `BreastSuper` が治療される。When MODをアンロードする / Then `m_HaanjaCanCure` が元の値へ戻る |
| AC-218 | FR-222, FR-224 | Given 感度と絶頂回数が蓄積した状態でセーブする / When ゲームを再起動して同じスロットをロードする / Then 保存時点の値へ戻る。Given 随伴ファイルのないスロット / Then 初期値で開始する |
| AC-219 | FR-223 | Given 書き込み中に異常終了する / When 次回ロードする / Then 部分書き込みのファイルが読まれず、直前の完全なファイルまたは初期値で開始する |
| AC-220 | FR-225 | Given 非対応スキーマ版の随伴ファイル / When ロードする / Then 読まれず、上書きもされず、検出した版と要求する版が提示される |
| AC-221 | FR-226 | Given 随伴ファイルの書き込みが失敗する / When プレイを続ける / Then ゲームと他の機構が継続し、失敗が記録される |
| AC-222 | FR-227 | Given 本MODだけ、SPEC002だけ、SPEC001だけの各構成 / When それぞれ起動する / Then いずれも起動に成功する |
| AC-223 | FR-228, FR-229 | Given 3つのMODを同時に動作させる / When ギャラリー、拘束、ゲームオーバーを一巡する / Then SPEC001のトリガーカタログから既存行が失われず、SPEC002の無力化窓と復帰遅延窓が従来どおり動作する |
| AC-224 | FR-230 | Given 拘束と絶頂が連続する場面 / When フレーム時間を計測する / Then MOD無効時に対して計測可能な範囲に収まり、待機によるフレーム停止がない |
| AC-225 | FR-231 | Given 任意の設定 / When 起動する / Then 有効な機構、確定済み調整値、随伴ファイルの位置、適用パッチ数がログに現れる |
| AC-226 | FR-232 | Given `Core` の単体テスト / When 快楽の増減、絶頂判定、限界算出、感度の単調性、随伴ファイルの直列化を検証する / Then ゲームを起動せずに合否が判定できる |
| AC-227 | FR-233 | Given 実測前の既定値のまま / When プレイする / Then 快楽が上昇せず絶頂も起きない。HP0抑止だけが有効である |
| AC-228 | FR-234 | Given `Enabled = false` / When 起動する / Then Harmonyパッチが1件も適用されず、随伴ファイルが書かれない |
| AC-230 | FR-235 | Given カタログファイルが存在しない構成で `SexualEnemyIds` に値がある / When 起動する / Then カタログが作成され、当該IDが `Sexual` として現れる。Given カタログが存在する / When `SexualEnemyIds` を変更して起動する / Then カタログの内容が変化しない |
| AC-231 | FR-236 | Given `Auto` の敵に拘束され快楽が上昇しない / When `F10` を押し当該敵を `Sexual` にして保存し、拘束を継続する / Then 再起動せずに次の被弾から快楽が上昇する |
| AC-232 | FR-237 | Given 一度も拘束されたことのない敵 / When 編集画面を開き全件表示に切り替える / Then 当該敵が一覧に現れ分類できる。Given 拘束された経験のある敵がある / Then それらが一覧の先頭に並ぶ |
| AC-233 | FR-238 | Given 編集画面で複数の敵の宣言を変更する / When `Escape` で閉じる / Then すべて開いた時点の値へ戻り、ファイルも変化しない |
| AC-234 | FR-239 | Given 非対応スキーマ版のカタログファイル / When 起動して編集を保存する / Then ファイルが上書きされず、拒否が記録され、ゲームと他の機構が継続する |

## 12. リポジトリ成果物と運用

| 成果物 | 位置 |
|---|---|
| プラグイン実装 | `src/SiNiSistar2.Pleasure.Plugin` |
| ゲーム非依存ロジック | `src/SiNiSistar2.Pleasure.Core` |
| 単体テスト | `tests/SiNiSistar2.Pleasure.Core.Tests` |
| 実行プラグイン | `BepInEx/plugins/community.sinisistar2.pleasure` |
| 設定ファイル（追跡しない） | `BepInEx/config/community.sinisistar2.pleasure.cfg` |
| 随伴ファイル（追跡しない） | `BepInEx/data/community.sinisistar2.pleasure/{slotKey}.json` |
| 敵別分類カタログ | `BepInEx/config/community.sinisistar2.pleasure/enemy-attacks.json` |
| 本仕様 | `docs/specifications/SPEC003.md` |
| 実装トレーサビリティ | `docs/implementation/SPEC003-traceability.md` |
| 実機テストシナリオ | `docs/testing/SPEC003-test-scenarios.md` |

3プロジェクトは `SiNiSistar2.Edi.sln` へ追加する。他MODのプロジェクトへ参照を追加しない。

### 12.1 導入順序

1. `dotnet test` と `dotnet build` を実行し、プラグインDLLを実行位置へ配置する。
2. ゲームを一度起動し、設定ファイルを生成させる。この時点で変化するのは拘束中のHP0抑止だけである。
3. 付録Aの実測を行い、調整値とゲームオーバーの発火手段を確定する。
4. 設定ファイルへ確定した値を記入し、`EnableClimaxGameOver` を有効化する。

### 12.2 ロールバック

`BepInEx/plugins/community.sinisistar2.pleasure` を削除する。または `Enabled = false` を設定する。ゲームのセーブデータとアセットへの変更は残らない。随伴ファイルは残るため、再導入すれば続きから再開できる。不要なら `BepInEx/data/community.sinisistar2.pleasure` を削除する。 敵別分類カタログは `BepInEx/config/community.sinisistar2.pleasure` に残る。

## 13. 設計判断

| ID | 判断 | 理由 | 採用しなかった案 |
|---|---|---|---|
| DEC-201 | HP0の抑止は `RemainHp1Msv` への寄与で行い、HPダメージ自体は止めない。 | ダメージを止めると拘束が無害に見え、脱出後の残HPも変わってしまう。止めるべきは「0へ達すること」だけである。`RemainHp1Msv` はゲーム自身の多重寄与機構であり、他の提供元と競合せず、寄与キーの解除だけで完全に元へ戻る。 | HPダメージの無効化、`HP` の毎フレーム書き戻し、`DisableDamageOnHp0` の強制 |
| DEC-202 | 拘束の敗北条件を快楽そのものではなく絶頂回数に置く。 | 快楽を敗北条件にすると1回の拘束で決着し、「何度も絶頂させられた末に力尽きる」という蓄積が表現できない。回数を挟むことで、1回の拘束の重さと、拘束を繰り返すことの重さが別々に調整できる。 | 快楽ゲージ自体を敗北条件にする、HPと快楽の複合条件 |
| DEC-203 | 性的攻撃の判別を、攻撃が付与する状態異常からの自動判別とし、敵IDの設定で上書きできるようにする。 | 全敵を列挙する案は工数が大きく、漏れた敵が無反応になる。攻撃が何を付与するかは、その攻撃の性質そのものを表す最も直接的な信号である。自動判別なら未知の敵にも既定の答えが出るため、ゲーム更新で敵が増えても壊れない。 | 敵IDの列挙のみ、拘束中は一律に上昇、`m_DamageTag` の文字列一致 |
| DEC-204 | 判別できない攻撃を非性的として扱う。 | 捕食・暴力を行う敵で快楽が上昇することは、要求に対する明確な誤りである。一方、性的な敵で上昇しないことは調整不足にとどまり、設定で修正できる。誤りの重さが非対称なので、安全側は非性的である。 | 判別不能を性的として扱う、判別不能時に上昇量を半分にする |
| DEC-205 | 絶頂演出をMOD独自のオーバーレイとする。 | 流用可能なゲーム資産があるかは実機調査が必要で、無ければ機構ごと成立しない。独自描画なら確実に出せ、資産の意味を取り違える危険もない。演出の質は後から改善できるが、成立するかどうかは先に確定させたい。 | ゲームの状態異常演出やフェードの流用、演出なし |
| DEC-206 | 感度と絶頂回数を、ゲームのセーブではなくセーブスロット単位の随伴ファイルへ保存する。 | ゲームのフラグ変数システムは `VariableType` の固定カテゴリと起動時に確定する固定長配列で構成され、新しい変数名を追加する手段がない。使うには未使用インデックスを占有するしかないが、未使用であることを確認する手段がなく、ゲーム更新でクエストやボスの進行状態を静かに破壊し得る。随伴ファイルはゲームのセーブ・ロードSubjectへ同期できるため、挙動は同等でありながらこの危険がない。 | `SaveFlagAndVariable` の未使用インデックスの占有、配列の拡張、セッション限定 |
| DEC-207 | 快楽を保存せず、感度と絶頂回数だけを保存する。 | 快楽は1回の拘束の中の状態であり、次のセッションへ持ち越す意味がない。保存すると「ロードした瞬間に絶頂する」状態を作れてしまう。 | 3つとも保存する、3つとも保存しない |
| DEC-208 | 感度に減少経路を一切設けない。 | 要求が明示的に一方通行を求めている。治療や休息で下がる逃げ道を作ると、蓄積そのものの意味が薄れる。上限は設けるが、これは成長の頭打ちであって減少ではない。 | 高難度の治療手段を用意する、時間経過で微減させる |
| DEC-209 | 絶頂限界の到達を、HP0抑止の寄与の解除として表現し、ゲームオーバー処理そのものは呼ばない。 | ゲームオーバーの発火は復帰処理、ペナルティ、セーブ、演出へ波及する。MODが自前で呼ぶと、そのすべてを正しく再現する責任を負い、誤れば進行不能やセーブ破損を招く。抑止を外すだけなら、以降はゲーム自身が普段どおりの経路を走る。MODが増やしたのは「いつHPが0へ到達し得るか」という条件だけであり、到達後の処理は一切変えていない。SPEC001の `game-over` トリガーが従来どおり成立するのも同じ理由による。 | `GameOverLabel.ExecutionOne` を自前の `GameOverParameter` で呼ぶ、`Lelia.RequestCommonDead` を立てる |
| DEC-210 | 性的とみなす状態異常の集合を、SPEC002の快楽系集合と一致させない。 | 目的が異なる。SPEC002は「抵抗の意思が損なわれる状態」を、本仕様は「性的な行為を受けたことを示す状態」を指す。`Defilement` はSPEC002では穢れ軸との二重作用を避けるため意図的に除外されるが、本仕様では性的攻撃の最も直接的な指標である。 | 両仕様で同じ集合を共有する、SPEC002から参照する |
| DEC-213 | `BreastSuper` の治療について、MODは搾乳シーンを自前で再生せず、ゲームの既存の治療経路が届くかを実測し、届かない場合に `m_HaanjaCanCure` を立てるにとどめる。 | 搾乳の映像は `SceneID.Ga_Player_Other_1_Breast2` として存在し、`GaTakePlayer.Play` も `DramaManager.PlayDrama` も公開されている。しかし前者はギャラリー用シーンと `GaTakePlayer` の実体の読み込みを前提とし、フィールド上では満たせない。ギャラリーモードへ入れて満たす案は、`GalleryManager.IsActive` が `LabelEvent.DisableWhenGallery` をはじめ広範な挙動を切り替えるため、進行中のランの状態を壊す危険が実利を上回る。`PlayDrama` は台詞だけを再生し映像を伴わない。一方、治療が届かない状態異常を新設することは治療不能な進行阻害であり、それだけは避けなければならない。`m_HaanjaCanCure` はゲーム自身の治療イベントに対象を伝えるだけで、治療処理そのものはゲームが行う。 | ギャラリーのテイクをフィールドで再生する、`ID_CureBreast_Player` の台詞だけを再生して治療扱いにする、MOD独自の治療手段を新設する、治療不能のまま出す |
| DEC-214 | 遷移の計数を「最大レベルの `Breast` へのさらなる付与」に限る。 | 付与のたびに数えると、レベルが上がっている最中にも計数が進み、ゲーム自身の段階表現が終わらないうちに上位へ飛ぶ。最大レベルの `Breast` が吸収できなくなった打撃だけが、`Breast` では足りないことの証拠である。最大レベルを読めない場合に遷移しないのも同じ理由で、未知の上限に対して数えれば初回で発火する。 | 付与回数を一律に数える、時間で数える、絶頂回数に紐づける |
| DEC-212 | 敵別の分類を設定ファイルの文字列から独立したカタログファイルへ移し、編集手段をゲーム内に置く。分類器はカタログを参照で保持する。 | 対象が108件あり、カンマ区切りの1行では読むことも差分を取ることもできない。さらに、その敵の攻撃が性的かどうかは画面に映っているものを見て下す判断であり、ゲームを終了してテキストを編集し再起動する経路では、判断の対象が目の前にないまま記憶で決めることになる。参照で保持するのは、値を複製すると編集が次回起動まで効かず、ゲーム内編集の意味が失われるためである。 | `ConfigEntry` の文字列を増やす、外部エディタ用のCSVのみを用意する、編集のたびにプロファイルを再構築する |
| DEC-211 | 本MODを第3のプラグインとして分離する。 | SPEC002は「セーブを書き換えない」を要件として固定しており、機構も難易度の強化に閉じている。本MODは新しいサブシステムであり、随伴ファイルという別の永続化責任を持つ。分離すれば片方だけを導入でき、一方の障害が他方を止めない。 | SPEC002のプラグインへ同居させる、SPEC002を拡張する |

## 14. 前提、延期事項、残存リスク

### 14.1 明示的な前提

- `StatusCondition.RemainHp1Msv` への寄与が、HPを `1` で停止させる効果を持つ。
- 拘束の成立と終了を `Lelia.IsHold` の遷移で検出できる。
- 拘束中の被弾を、受け側がプレイヤーであるダメージ解決として観測できる。
- `MainSaveData.SelectID` と `LoadedFileName` が、セーブスロットを一意に識別するのに十分である。
- `SaveData` のセーブ・ロードSubjectが、実際のファイル入出力と整合する順序で発火する。
- `AbnormalType.BreastSuper` を通常の付与経路で適用でき、ゲームが破綻しない。
- 性的・非性的の分類は状態異常名からの推論であり、実機のプレイ感で見直す。

### 14.2 延期事項

- 快楽・感度・絶頂回数のゲーム内表示のデザイン改善。当面は最小限のオーバーレイとする。
- 絶頂演出の音響。
- 敵ごと・攻撃ごとの快楽上昇量の個別調整。当面は一律とする。
- 感度が快楽以外へ与える影響（移動、抵抗、状態異常の付きやすさ）。
- 絶頂回数に応じた見た目やセリフの変化。
- 随伴ファイルのセーブファイル移動・コピーへの追随。
- `BreastSuper` 以外のイベント限定コンテンツの開放。
- 既存セーブへの感度の遡及付与。

### 14.3 残存リスク

| リスク | 影響 | 軽減策 |
|---|---|---|
| ゲームオーバーの発火手段を特定できない | 拘束による敗北が成立せず、拘束が実質無害になる | FR-216により発火しないまま成立条件を記録する。付録A A-4で手段を確定するまで `EnableClimaxGameOver` を無効に保つ。特定できない場合は延期事項へ移し、HP0抑止を無効化して従来の敗北条件へ戻す選択肢を残す |
| 拘束中の被弾を観測できない | 快楽が上昇せず、機構全体が成立しない | 付録A A-2の実測項目とする。被弾が観測できない場合、拘束の継続時間による上昇へ縮退する案を検討する |
| 性的・非性的の分類が実態と合わない | 捕食する敵で快楽が上昇する、または性的な敵で上昇しない | DEC-203により敵ID設定で上書きできる。DEC-204により既定は安全側（非性的）に倒す |
| `RemainHp1` 寄与の解除漏れ | 拘束外でも不死化し、ゲームが成立しなくなる | FR-204で3つの解除契機を規定し、AC-203で検証する。解除失敗時は当該機構を無効化する |
| 随伴ファイルとセーブの不一致 | 感度・絶頂回数が別のプレイのものになる | スロットキーに `SelectID` と `LoadedFileName` の両方を持たせ、ゲームビルド識別子の不一致を記録する。セーブファイルの手動コピーは14.2の延期事項とする |
| `BreastSuper` が通常プレイを想定していない | 進行不能、演出の破綻、セリフの文脈崩れ | `BreastSuperChance` の既定を `0` とし、実機で確認してから有効化する。付与は正規経路のみとし、解除できる状態として成立させる |
| SPEC002と重なって拘束が過度に長引く | 体感が単調になる、プレイが停滞する | 7.3に相互作用を明示する。3つのMODは独立に無効化でき、片方だけの構成も正当である |
| 絶頂演出がゲームのUIと重なる | 視認性の低下 | 描画位置と不透明度を設定可能にする（延期事項）。当面は画面端を避けた最小限の表示とする |
| ゲーム更新でパッチ対象が変わる | 機構が無言で効かなくなる | 9章でパッチ対象の欠落を当該機構の無効化と提示として扱う。FR-231の起動ログに適用パッチ数を出す |
| 感度が減らないため詰みが発生する | 高感度のセーブで進行不能になる | 絶頂回数はセーブポイントでリセットされるため、詰みは絶頂限界が耐えられない場合に限られる。限界は耐久で伸びるため、育成で解消できる。解消できない構成が見つかった場合は `SensitivityCap` と `ClimaxLimitPerDurability` の調整で対応する |

## 付録A: 実測で確定する項目

対象ビルドのIL2CPPネイティブ実装はinteropアセンブリから読めない。次の項目は**推論であり、調整値と有効化の前に実機で確認する**。手順は `docs/testing/SPEC003-test-scenarios.md` に記録する。

| # | 確認項目 | 確認できないと影響する箇所 |
|---|---|---|
| A-1 | `RemainHp1Msv` への寄与が実際にHPを `1` で停止させるか。他の提供元と競合しないか | 5.1、FR-203。最重要 |
| A-2 | 拘束中の被弾を、受け側がプレイヤーであるダメージ解決として観測できるか。スリップダメージも観測できるか | 5.2、FR-207。観測できないと快楽が上昇しない |
| A-3 | 拘束中の被弾で `DamageParameter.m_AbnormalTypes` が実際に埋まっているか。埋まっていない攻撃の割合 | 5.3、DEC-203の前提 |
| A-4 | ~~ゲームオーバーを安全に発火する手段~~ **解消済み（2026-08-08）。** HP0抑止の寄与を解除し既存経路へ委ねる方式を採用したため、新たな発火手段を必要としない | 5.5、FR-215 |
| A-5 | ~~5.5のゲームオーバーがHP0を経由するか~~ **解消済み（2026-08-08）。** 経由する。SPEC001の `game-over` トリガーは従来どおり発生する | 7.2 |
| A-6 | `PlayerStatusManager.m_MaxDurability` の値域と、育成による増分 | 5.5、`ClimaxLimitPerDurability` の既定値 |
| A-7 | ~~セーブポイントのアクティベートを検出する手段~~ **解消済み（2026-08-08）。** `SavePointAsyncLabel.ExecutionOneAsync` を検出し、`IsObeliskLabel` でオベリスクを判別する。残る確認は、当該メソッドがシーン設定時ではなく作動時に走ることの実機確認のみ | 5.6、FR-217 |
| A-8 | `SaveData` のセーブ・ロードSubjectの発火順序と、実際のファイル入出力との前後関係 | 5.9、FR-222 |
| A-9 | `MainSaveData.LoadedFileName` がオートセーブと手動セーブで安定しているか | 5.9のスロットキー |
| A-10 | `AbnormalType.BreastSuper` を通常付与したときの挙動。演出、UI、セリフ、解除可否 | 5.8、FR-221 |
| A-14 | ~~`Breast` の `MaxLevel`~~ **解消済み（2026-08-08）。`1` である。** 残るのは `BreastSuper` の `MaxLevel`、両者の `HaanjaCanCure` と `PhysicalConditionFlag`（付与済みの実体で読む）、および `Breast` の治療イベントの条件 | 5.8、FR-241。治療が届くかを決める |
| A-15 | 膨乳を付与するアイテムが `AddAbnormal(AbnormalType)`、`AddAbnormal(AbnormalData)`、`AddOrRemoveAbnormal` のどれを通るか | 5.8、FR-244 |
| A-13 | 拘束中に編集画面を開いたときの入力の干渉。キー入力とクリックがゲーム側へ同時に届くことによる実害の有無 | 5.11、FR-236 |
| A-11 | 絶頂演出のオーバーレイがゲームのUIやSPEC001のホールドUIと競合しないか | 5.4、14.3 |
| A-12 | 快楽の上昇量、減衰量、感度の増分、絶頂限界の基礎値の実用域 | 6章の「実測後に確定」全項目 |

## 付録B: 参照した interop シンボル

本仕様が参照するシンボルは `BepInEx/interop/SiNiSistar2.dll` に実在することを確認済みである。意味論の確度は付録Aによる。

| 領域 | シンボル |
|---|---|
| HP0抑止 | `SiNiSistar2.Obj.StatusCondition.RemainHp1Msv`（`MultiSettingValue<bool>`）、`RemainHp1`、`SiNiSistar2.Obj.MultiSettingValue<T>`（`ResitValue`、`ReleaseValue`） |
| 敗北状態 | `SiNiSistar2.Obj.LeliaDeadState`（`Live`/`HP0`/`GameOver`）、`Lelia.IsHP0`、`Lelia.RequestCommonDead`、`Lelia.AcceptCommonDead`、`SiNiSistar2.EventLabel.GameOver.GameOverLabel`、`GameOverParameter`、`HpControlType` |
| 拘束 | `SiNiSistar2.Obj.Lelia.IsHold`、`Bind.IsBound`、`Bind.BinderEnemy`、`Bind.OnReleaseResponse` |
| 被弾と判別 | `SiNiSistar2.Damage.DamageStack.IsReceiverLelia`、`m_DamageParameter`、`DamageParameter.m_AbnormalTypes`、`SiNiSistar2.UI.Gallery.EnemyData.GalleryEnemyID` |
| 状態異常 | `SiNiSistar2.Obj.AbnormalList.AddAbnormal`、`Has`、`GetAbnormalLevel`、`AbnormalType.BreastSuper`、`PlayerStatusManager.IsBreast` |
| ステータス | `SiNiSistar2.Obj.PlayerStatusManager.m_MaxDurability`、`Durability` |
| セーブ | `SiNiSistar2.Manager.SaveData.OnAfterLoadMainSaveData`、`OnBeforeSaveMainSaveData`、`OnAfterSaveMainSaveData`、`SaveDataFolderPath`、`MainSaveData.SelectID`、`LoadedFileName`、`LoadedFilePath` |
| セーブポイント | `SiNiSistar2.Obj.SavePointSelector`、`SiNiSistar2.UI.SavePointMenu`、`SiNiSistar2.EventLabel.SavePointAsyncLabel` |
| 採用しなかった保存先 | `SiNiSistar2.Manager.SaveFlagAndVariable`、`FlagAndVariableStatus.SetVariable`、`VariableType`（`Global`/`Quest`/`Item`/`SystemGlobal`/`Boss`/`Dungeon`） |
| 他MODが管理し触れない面 | `GachaGachaSystem`、`GachaGachaParameter`、`PlayerStatusManager.MoveSlowRateMsv`、`IsHardMode`、`s_GameDifficultyForCheck` |
