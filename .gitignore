# .exe
*.exe

# resource files
SiNiSistar2_Data/Resources
SiNiSistar2_Data/StreamingAssets

# media files
Mod/Player

# .NET build outputs
**/bin/
**/obj/
artifacts/

# BepInEx runtime outputs (runtime dependencies and plugin DLLs stay tracked)
BepInEx/cache/
BepInEx/diagnostics/
BepInEx/LogOutput*.log
BepInEx/ErrorLog.log
BepInEx/config/*.cfg

# MOD-written save-adjacent data
BepInEx/data/
scripts/models/

# scripts/ 用の Python 仮想環境（グローバル汚染を避けるため venv 管理。README.md 参照）
scripts/.venv/

# Local safety copies of the game executable. Two of these (210 MB each) reached the
# history once and made the branch unpushable; they are working files, not versioned ones.
Edi/*.bak-*
Edi/Edi.exe.bak*
